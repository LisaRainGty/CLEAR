# Raw corpus (optional)

Place or symlink the ~34GB raw livestream corpus here:
`comment/`, `srt_cut/`, `product_images/`.

Not required to reproduce paper tables from the released supervised jsonl.
On the author machine this layer lives at `../data/raw` (sibling of `main/`).
